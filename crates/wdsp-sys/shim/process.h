/* Intercepted <process.h> — only _beginthread is needed.
 * wdsp_posix.h already declares it as a static inline that delegates to the
 * POSIX helper. */
#ifndef WDSP_SHIM_PROCESS_H
#define WDSP_SHIM_PROCESS_H
#include "wdsp_posix.h"
#endif
